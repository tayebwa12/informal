name:   informal

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4


      - name: Deploy to Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 216.104.197.76
          username: ${{ secrets.SERVER_USER }}
          key: ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPp+NAoWhfDPPnFSvJIg6eUL84ckyFq5p/UxpttpnTd4 tayebwayonah7@gmail.com
          script: |
            cd ${{ secrets.SERVER_PATH }}
            git pull origin main
